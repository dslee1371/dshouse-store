<h2 style="margin-bottom:6px">결제(계좌송금)</h2>
<p class="muted" style="margin-top:0">주문번호: <strong>#<%= orderId %></strong></p>

<div class="notice-card">
  <div class="title">계좌 정보</div>
  <div>계좌번호: <span class="bank">농협 241097-56-078780 장은애</span> <button class="btn ghost" id="copyBtn" type="button">복사</button></div>
  <div style="margin-top:6px;color:var(--muted)">입금 확인 후 발송 일정을 안내드립니다. 평균 1주 소요.</div>
</div>

<div style="margin-top:18px">
  <p>주문상품: <strong><%= productTitle %></strong></p>
  <% if (optionText) { %>
    <p>선택옵션: <strong><%= optionText %></strong></p>
  <% } %>
  <p>결제금액: <strong><%= amount.toLocaleString() %>원</strong></p>
  <p>입금자명: <strong><%= buyerName %></strong> (다르면 메모에 주문번호를 적어주세요)</p>
</div>

<p style="margin-top:16px"><a class="btn brand" href="/">쇼핑 계속하기</a></p>

<script>
  (function(){
    const copyBtn = document.getElementById('copyBtn');
    copyBtn?.addEventListener('click', async () => {
      try { await navigator.clipboard.writeText('농협 241097-56-078780 장은애'); copyBtn.textContent = '복사됨'; setTimeout(()=> copyBtn.textContent='복사', 1500); } catch(e){ alert('복사 실패'); }
    });
  })();
</script>